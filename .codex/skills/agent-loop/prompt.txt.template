Read @agent-loop-instructions.md and follow it. Your assigned issue is #{ISSUE_ID}. Run `gh issue view {ISSUE_ID}` for the description. Implement, run focused validation, refactor, and create a local commit. Do not push, open a pull request, or invoke hosted AI reviewers; the wrapper owns local Claude/Codex review, fresh-base integration, publication, and PR creation. If you bail, follow the instructions' classification protocol before exiting.
